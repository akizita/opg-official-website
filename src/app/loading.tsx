export default function Loading() {
  return (
    <div className="container status-page" role="status">
      <p className="eyebrow">Loading</p>
      <p>Please wait while the page is prepared.</p>
    </div>
  )
}
